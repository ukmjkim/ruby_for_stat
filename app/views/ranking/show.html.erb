<p>
  <strong>Title:</strong>
  <%= @ranking.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @ranking.text %>
</p>

<h2>Comments</h2>
<%= render @ranking.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_ranking_path(@ranking) %> |
<%= link_to 'Back', ranking_index_path %>
